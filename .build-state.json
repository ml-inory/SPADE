{
  "goal": "Apply SPADE to the real CosyVoice 2 (0.5B) LLM-TTS backbone: WER-based layer importance (WLI) pruning of its 24-layer Qwen2 LLM plus adaptive multi-level distillation recovery on a small real-audio dataset, with Whisper-WER / RTF / parameter evaluation.",
  "stack": "python (torch 2.9 + 2x NVIDIA L4 24GB; CosyVoice upstream repo + CosyVoice2-0.5B checkpoint)",
  "base_branch": "main",
  "branch": "feat/build-cosyvoice2-20260818",
  "iteration": 1,
  "validation_command": "python -m pytest -x -q",
  "requirement_alignment": {
    "source": "Derived from user request ('先做CosyVoice2') + the SPADE paper (CosyVoice2 rows of Table 1: 24 -> 12/9 layers) + inspection of the official CosyVoice repo (Qwen2LM wraps Qwen2ForCausalLM; llm.pt/flow.pt/hift.pt checkpoint; parquet data pipeline).",
    "goal": "Runnable SPADE-on-CosyVoice2 integration: load the official checkpoint, compute WLI over LLM layers, prune 24->12, distill with the SPADE composite loss on a small real dataset, and evaluate WER/RTF/params before and after.",
    "non_goals": [
      "Full reproduction of the paper's 7-epoch LibriTTS-scale run (limited to a small demo subset here)",
      "DPO / GRPO, streaming inference, TRT/JIT export, CosyVoice 3",
      "Pruning flow/hift (paper prunes only the LLM backbone)"
    ],
    "user_visible_behavior": [
      "Setup script that prepares the CosyVoice repo, Python deps, and pretrained checkpoint",
      "CLI stages: prepare-data, prune-llm, wli, distill, evaluate (under spade_cosyvoice2/)",
      "WLI report for all 24 LLM layers + pruned checkpoint + distilled checkpoint",
      "README section documenting the CosyVoice2 pipeline"
    ],
    "constraints": [
      "Must not modify the upstream CosyVoice repo; integration lives in the SPADE repo and imports CosyVoice via PYTHONPATH",
      "Keep demo data small (LibriTTS dev-clean subset, ~54MB) so the whole flow runs on 2x L4",
      "Pruning touches only the Qwen2 layer stack; all other LLM modules (text encoder, embeddings, decoder) are copied"
    ],
    "risks": [
      "CosyVoice upstream code/API drift vs the paper's version (pinned at download time)",
      "Speech-tokenizer / campplus onnx runtime issues (onnxruntime CPU/GPU providers)",
      "WLI cost: 24 layers x eval subset x (TTS synth + Whisper) -- controlled via small eval subset",
      "Model download from hf-mirror was failing; using modelscope (verified ~2MB/s)"
    ],
    "acceptance_checks": [
      "python -m pytest -x -q still passes (existing suite)",
      "Zero-shot inference synthesizes audio with the official checkpoint",
      "Pruned 12-layer LLM loads and synthesizes; params/depth reduced",
      "WLI report computed over the 24 LLM layers on an eval subset",
      "Distillation improves WER vs the pruned model on the eval subset",
      "README documents the CosyVoice2 flow"
    ],
    "validation_approach": "Stage-level runs on the real checkpoint + existing pytest suite; final numbers recorded in a report JSON",
    "first_iteration_scope": "spade_cosyvoice2/ package skeleton + setup script + docs; verify checkpoint loads and zero-shot inference works",
    "follow_up_iterations": [
      "Data prep (LibriTTS dev-clean -> parquet with embeddings + speech tokens)",
      "LLM pruning (24->12) via state-dict remap + inference check",
      "WLI computation on the real model",
      "SPADE distillation training + evaluation",
      "End-to-end runner + README + PR"
    ],
    "assumptions": [
      "User wants the paper's CosyVoice2 experiment (LLM depth pruning + distillation) runnable in this environment",
      "Prune only the LLM Qwen2 layers; flow/hift untouched",
      "Small real dataset (LibriTTS dev-clean subset) is acceptable for a demo run; scaling knobs documented",
      "WLI uses Whisper ASR WER on synthesized audio, per the paper",
      "GitHub issues/PRs on ml-inory/SPADE are authorized (previous run established)"
    ],
    "open_questions": [
      "Target depth 12 vs 9 (paper reports both) -- default 12, configurable"
    ]
  },
  "issues": [
    {
      "number": 7,
      "title": "feat[cv2-1]: CosyVoice2 integration skeleton, setup script, and smoke inference",
      "acceptance_checks": [
        "spade_cosyvoice2/ package exists with path/config helpers",
        "setup.sh prepares CosyVoice upstream, Python deps, and downloads CosyVoice2-0.5B (modelscope, with HF fallback)",
        "smoke.py loads CosyVoice2 and synthesizes one zero-shot utterance to a wav (exit 0)",
        "Existing pytest suite still passes"
      ],
      "url": "https://github.com/ml-inory/SPADE/issues/7"
    },
    {
      "number": 8,
      "title": "feat[cv2-3]: Prune the CosyVoice2 LLM (24 -> 12 Qwen2 layers)",
      "acceptance_checks": [
        "spade_cosyvoice2/prune_llm.py prunes llm.pt (24 -> target) via remap_layer_keys and saves a loadable checkpoint + retained-indices json",
        "load_cosyvoice2_with_llm loads the pruned checkpoint into a shrunken Qwen2 and synthesizes speech (verified on the official checkpoint)",
        "Unit tests cover layer counting, key remapping, default retention, and checkpoint roundtrip"
      ],
      "url": "https://github.com/ml-inory/SPADE/issues/8"
    }
  ],
  "rounds": [
    {
      "iteration": 1,
      "issue": 7,
      "changed_files": [
        "spade_cosyvoice2/__init__.py",
        "spade_cosyvoice2/paths.py",
        "spade_cosyvoice2/setup.sh",
        "spade_cosyvoice2/smoke.py",
        "spade_cosyvoice2/model_utils.py",
        ".build-state.json"
      ],
      "test_status": "pass",
      "test_command": "python -m pytest -x -q",
      "test_summary": "38 existing tests pass; smoke.py exit 0 with a 6.24s synthesized wav on the official CosyVoice2-0.5B checkpoint (2x L4)",
      "implementation_summary": "Scaffolded spade_cosyvoice2/ with path helpers, a one-shot setup script (CosyVoice upstream + deps + modelscope/HF checkpoint download), shared model/data utils, and a smoke CLI. Worked around a torchaudio 2.9/torchcodec breakage by patching cosyvoice load_wav to soundfile.",
      "commit_hash": null
    },
    {
      "iteration": 3,
      "issue": 8,
      "changed_files": [
        "spade_cosyvoice2/prune_llm.py",
        "tests/test_cosyvoice2_prune.py",
        ".build-state.json"
      ],
      "test_status": "pass",
      "test_command": "python -m pytest -x -q",
      "test_summary": "44 tests pass; verified on the official checkpoint: llm.pt 24 -> 12 layers (retained [0,7-16,23], 3.15B->315M params in Qwen2 stack), pruned checkpoint loads and synthesizes speech",
      "implementation_summary": "prune_llm.py: default even-spaced retention + WLI-driven retained list support, state-dict remap via spade.adapters.hf.remap_layer_keys, and shrink_qwen2_for_llm/load_pruned_llm_state helpers used by load_cosyvoice2_with_llm. Verified end-to-end on the real CosyVoice2-0.5B checkpoint.",
      "commit_hash": null
    }
  ],
  "pre_existing_changes": [],
  "assumptions": [
    "Derived from requirement_alignment.assumptions (see above)"
  ]
}
